If any files that are too large, create dedicated directory for it, and include sub-directories (depending on the size of the initial file!) and sub-files inside of the directory.

Modularize and compartementalize the logic into sections